## Automatically analyze the result when running multiple system commands in R #irl

Libraries:
- RPostgreSQL: connect DB
- openxlsx : save output to a XLSX file
- log4r: log the *stdout* & *stderr*
- stringr: substring, matching